/*
 * JS for A_Powertrain generated by Appery.io
 */
Apperyio.getProjectGUID = function() {
    return 'ec7518f9-d042-4137-bf4f-6bad235470fc';
};

function navigateTo(outcome, useAjax) {
    Apperyio.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Apperyio.adjustContentHeightWithPadding();
}

function adjustContentHeightWithPadding(_page) {
    Apperyio.adjustContentHeightWithPadding(_page);
}

function setDetailContent(pageUrl) {
    Apperyio.setDetailContent(pageUrl);
}
Apperyio.AppPages = [{
    "name": "medical",
    "location": "medical.html"
}, {
    "name": "startScreen",
    "location": "startScreen.html"
}, {
    "name": "Products_Entertainment_system",
    "location": "Products_Entertainment_system.html"
}, {
    "name": "Screen1",
    "location": "Screen1.html"
}, {
    "name": "automotive",
    "location": "automotive.html"
}, {
    "name": "products",
    "location": "products.html"
}, {
    "name": "A_Entertainment_Systems",
    "location": "A_Entertainment_Systems.html"
}, {
    "name": "datacom",
    "location": "datacom.html"
}, {
    "name": "A_Powertrain",
    "location": "A_Powertrain.html"
}];

function A_Powertrain_js() {
    /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
    };
    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }
    /*
     * Nonvisual components
     */
    Apperyio.mappings = Apperyio.mappings || {};
    Apperyio.datasources = Apperyio.datasources || {};
    Apperyio.CurrentScreen = 'A_Powertrain';
    _.chain(Apperyio.mappings)
        .filter(function(m) {
            return m.homeScreen === Apperyio.CurrentScreen;
        })
        .each(Apperyio.UIHandler.hideTemplateComponents);
    /*
     * Events and handlers
     */
    // On Load
    var A_Powertrain_onLoad = function() {
        A_Powertrain_elementsExtraJS();
        A_Powertrain_deviceEvents();
        A_Powertrain_windowEvents();
        A_Powertrain_elementsEvents();
    };
    // screen window events
    function A_Powertrain_windowEvents() {
        $('#A_Powertrain').bind('pageshow orientationchange', function() {
            var _page = this;
            adjustContentHeightWithPadding(_page);
        });
    };
    // device events
    function A_Powertrain_deviceEvents() {
        document.addEventListener("deviceready", function() {
        });
    };
    // screen elements extra js
    function A_Powertrain_elementsExtraJS() {
        // screen (A_Powertrain) extra code
    };
    // screen elements handler
    function A_Powertrain_elementsEvents() {
        $(document).on("click", "a :input,a a,a fieldset label", function(event) {
            event.stopPropagation();
        });
    };
    $(document).off("pagebeforeshow", "#A_Powertrain").on("pagebeforeshow", "#A_Powertrain", function(event, ui) {
        Apperyio.CurrentScreen = "A_Powertrain";
        _.chain(Apperyio.mappings)
            .filter(function(m) {
                return m.homeScreen === Apperyio.CurrentScreen;
            })
            .each(Apperyio.UIHandler.hideTemplateComponents);
    });
    A_Powertrain_onLoad();
};
$(document).off("pagecreate", "#A_Powertrain").on("pagecreate", "#A_Powertrain", function(event, ui) {
    Apperyio.processSelectMenu($(this));
    $(".counter").html(pocet);
    A_Powertrain_js();
});